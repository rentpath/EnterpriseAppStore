<h1>Editing project <%= @project.name %></h1>

<%= render 'form' %>

<%= link_to 'Versions', project_app_versions_path(@project) %> | 
<%= link_to 'Show', @project %> |
<%= link_to 'Back', projects_path %>